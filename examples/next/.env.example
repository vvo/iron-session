# Copy to .env.local for local development.
# Generate real values with: openssl rand -base64 32
#
# These are demo values. Never reuse them in an app that matters: anything
# checked into a public repo is public.
SESSION_PASSWORD=dev_only_session_password_change_me_32
MAGIC_LINK_PASSWORD=dev_only_magiclink_password_change_it
